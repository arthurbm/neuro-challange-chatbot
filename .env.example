# =============================================================================
# CREDIT ANALYTICS CHATBOT - Environment Variables
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores reais
# cp .env.example .env

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Escolha um dos cenários abaixo:

# OPÇÃO 1: Local Development (PostgreSQL local)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=credit_analytics
# DB_USER=chatbot_reader
# DB_PASSWORD=chatbot_pass_dev

# OPÇÃO 2: Google Cloud SQL (Produção - usado no LangGraph Cloud)
DB_HOST=34.30.106.127  # IP público da instância Cloud SQL
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres  # ou chatbot_reader para read-only
DB_PASSWORD=your-cloud-sql-password

# OPÇÃO 3: AWS RDS (Alternativa)
# DB_HOST=your-rds-instance.region.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=credit_analytics
# DB_USER=chatbot_reader
# DB_PASSWORD=your-secure-password

# Connection pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_STATEMENT_TIMEOUT=10000  # 10 seconds in milliseconds

# -----------------------------------------------------------------------------
# LLM CONFIGURATION
# -----------------------------------------------------------------------------
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.0
LLM_MAX_TOKENS=2000

# OpenAI API Key
OPENAI_API_KEY=sk-your-openai-api-key-here

# Para usar outros providers via OpenRouter:
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
# OPENAI_API_KEY=sk-or-v1-your-openrouter-key

# -----------------------------------------------------------------------------
# LANGSMITH CONFIGURATION
# -----------------------------------------------------------------------------
# IMPORTANTE: No deployment do LangSmith Studio, o control plane define automaticamente
# LANGSMITH_PROJECT com o nome do deployment (e.g., "neuro-challange-chatbot-v3").
# Para desenvolvimento local, você pode descomentar LANGSMITH_PROJECT abaixo.
# No deployment, deixe comentado para usar o nome automático.
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=lsv2_pt_your-langsmith-api-key
# LANGSMITH_PROJECT=credit-chatbot-dev  # Opcional - automático no deployment
LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# -----------------------------------------------------------------------------
# GUARDRAILS & SECURITY
# -----------------------------------------------------------------------------
# K-anonimato: grupos com menos de K observações serão filtrados
K_ANONYMITY=20

# Limites de queries
DEFAULT_QUERY_LIMIT=100
MAX_ROWS_RETURN=10000

# Timeout
QUERY_TIMEOUT_SECONDS=10

# Retry
MAX_RETRY_ATTEMPTS=3

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# Ambiente: development, staging, production
ENVIRONMENT=development

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# GOOGLE CLOUD STORAGE (Para visualizações)
# -----------------------------------------------------------------------------
GCS_PROJECT_ID=neuro-test-476419
GCS_BUCKET_NAME=neuro-test

# Opção 1 (RECOMENDADA): JSON como string (para produção/deploy)
# GCS_SERVICE_ACCOUNT_JSON_CONTENT='{"type":"service_account","project_id":"...","private_key":"..."}'

# Opção 2: Path para arquivo JSON (para desenvolvimento local)
# GCS_SERVICE_ACCOUNT_JSON=/path/to/service-account-key.json

# Opção 3: Application Default Credentials (deixe ambas vazias)
# Requer: gcloud auth application-default login

# -----------------------------------------------------------------------------
# AWS CONFIGURATION (Optional - para produção)
# -----------------------------------------------------------------------------
# AWS_REGION=sa-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
